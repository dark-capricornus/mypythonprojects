#Write a program that takes the length and width of a rectangle as input from the user and
#calculates the area using the formula: area = length * width. Display the result.

length = int(input("Length : "))
width = int(input("Width : "))
area=0
area=length*width
print (area)
